# Diretório que contém models.ini e o cache dos modelos do llama.cpp.
MODELS_DIR=/absolute/path/to/models

# Gere um valor aleatório: openssl rand -hex 32
WEBUI_SECRET_KEY=replace-with-a-random-secret
